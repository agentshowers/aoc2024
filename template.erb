require "./lib/parser.rb"
require "./lib/utils.rb"
require "./lib/base.rb"

class Day<%= day %> < Base
  DAY = <%= day %>


  def initialize(type = "example")
    lines = Parser.lines(DAY, type)
    lines.each do |line|
      # a,b,c = line.scan(/[a-zA-Z]+|\d+/)
      # a,b,c = line.scan(/\d+/).map(&:to_i)
    end
  end

  def one
    1
  end

  def two
    2
  end
end